# Minimal local agent configuration for this repository.
# Keep delegation shallow so feature context stays easy to review.

[agents]
max_depth = 1

[mcp_servers.tabulareditor-cli]
command = "uv"
args = ["--directory", "C:\\Users\\gamer\\source\\repos\\TabularEditor\\tools\\mcp", "run", "server.py"]
enabled = true
startup_timeout_sec = 10
tool_timeout_sec = 60
